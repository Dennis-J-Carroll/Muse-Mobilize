# KingsBlood source profile

This is a preparation profile, not a copy of canon or permission to import. Confirm it against the source's current `AGENTS.md` and relevant lore conventions.

- `lore/` is the source knowledge base; do not reorganize it into `kb/`.
- Structured lore pages outrank an overlapping compendium when they conflict.
- `foundation`, `developing`, and `stub` describe document maturity. LOCKED describes claims. Never equate those axes.
- Preserve CONTESTED accounts, SELF-PERCEPTION vs. TRUTH, OPEN GROUND, working names, and voice/agent seeds verbatim. Do not execute those seeds or fill intentional gaps during import.
- Exclude `archive/`, superseded material, `[PLAY]` transcripts, and unreviewed sessions from automatic canon selection. Path alone is insufficient: `Write/KB_explainv1.txt` is identified as superseded by source conventions.
- Keep `(NEW LORE)`, `status: unreviewed`, and unresolved spelling/alias distinctions. Consult current `lore/vocab.md` before interpreting identities. Surface source disagreements rather than normalizing them.

The existing proposed pilot selects these paths, subject to a current preview and writer approval:

1. `lore/families/characters/dermelius_royer.md` → source document under `canon`, maturity `foundation` if still evidenced.
2. `lore/kingdoms/jandell.md` → source document under `canon`, maturity `stub` if still evidenced.
3. `Write/The_Load_They_Bear.md` → manuscript, no invented scene chronology/status.

Version 1 does not generate character/location cards or extract facts from these documents. Do not infer geography, identity matches, or authority not present in source evidence. Source repo stays read-only.
